name: CI

on: [push]

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  ci:
    runs-on: [self-hosted, linux, ARM64]
    steps:
      - uses: actions/checkout@v3

      - uses: ./.github/actions/install

      - run: yarn lint
